# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Secrets (never commit)
service_account.json
*.key

# OS files
.DS_Store
*.tmp
*.log

# Data folder
data/photos/          # Large photo files (not needed for hosting)
data/cache/           # Thumbnail cache (regenerated automatically)

# Keep these (needed for hosting):
# - data/fonts/              (needed for UI)
# - data/drive_mapping.json  (needed for image URLs)
# - data/embeddings/         (needed for face matching)
data/cache/
